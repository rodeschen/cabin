'use strict';
define(['cabin'], function(cabin) {
    return ['controller', 'favoriteCtrl', ['$scope', 'favoriteServ', '$q', '$timeout', '$resource',
        function($scope, serv, $q, $timeout, $resource) {

        }
    ]];

});
