'use strict';
define(['cabin'], function(cabin) {
        return ['controller', 'appCtrl', ['$scope', '$interval',
                function($scope, $interval) {
                    

                ]
            ];
        });
